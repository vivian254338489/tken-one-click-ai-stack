# Setup

1. Copy `configs/litellm.config.yaml`.
2. Replace `your_tken_api_key`.
3. Keep API base URL as:

```text
https://www.tken.shop/v1
```

4. Use `free-model` by default.
5. Escalate to `premium-gpt` for hard coding or failed schema extraction.
