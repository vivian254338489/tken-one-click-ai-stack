# Short Video Scripts

Use for YouTube Shorts, TikTok, Reels, and Bilibili-style vertical videos. Keep each video 25-45 seconds.

## Script 1: 3-Minute Gateway

Title:

```text
I built a ChatGPT API gateway in 3 minutes
```

Structure:

1. Show result in first 5 seconds:
   - Browser opens ChatGPT-style UI.
   - Send one prompt.
   - Show model route selector.
2. Pain:
   - "Every AI app starts simple, then suddenly you need GPT, Claude-style UI, cheap routes, and client configs."
3. Solution:
   - "This one-click stack gives one OpenAI-compatible endpoint, Docker/Railway/Vercel deploy files, and web UIs."
4. CTA:
   - "Search TKEN one-click AI stack on GitHub or use tken.shop."

## Script 2: Stop Paying Premium For Every Task

Title:

```text
Stop sending every AI task to your most expensive model
```

Structure:

1. Show table: extraction, drafts, coding, final answers.
2. Pain:
   - "Most teams use one premium model for everything because routing is annoying."
3. Solution:
   - "Use local routes: `free-model` for drafts, `premium-gpt` for hard reasoning."
4. CTA:
   - "I put the gateway template on GitHub. Link in profile."

## Script 3: Chinese Models For Overseas Users

Title:

```text
Why overseas AI users are testing Chinese model routes
```

Structure:

1. Show one endpoint: `https://www.tken.shop/v1`.
2. Pain:
   - "People want cheaper routes, but do not want to rebuild their app or fight unfamiliar payment flows."
3. Solution:
   - "Use OpenAI-compatible routing. Keep premium GPT for hard tasks. Use low-cost routes for simple volume."
4. CTA:
   - "Try the demo stack before moving real traffic."

## Script 4: Claude-Style UI From One API

Title:

```text
Claude-style UI, OpenAI-compatible backend
```

Structure:

1. Show Claude-style web UI.
2. Pain:
   - "You do not need to rebuild the backend just to test a different chat interface."
3. Solution:
   - "Run the standalone Claude-style package and point it to the same `/v1` API."
4. CTA:
   - "Download the TKEN one-click kit on GitHub."
