# Setup

1. Copy `configs/continue.config.json`.
2. Replace `your_tken_api_key`.
3. Keep API base URL as:

```text
https://www.tken.shop/v1
```

4. Start with `free-model`.
5. Use `premium-gpt` only when the task needs stronger reasoning.

Get a key:

```text
https://www.tken.shop/?utm_source=github&utm_medium=docs&utm_campaign=tken_vscode_continue_config_pack
```
